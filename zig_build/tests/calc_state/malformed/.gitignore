# The corpus is regenerated deterministically by generate_corpus.py from the
# tracked c47Test.sav, and no build step consumes it yet (M-SAFE-7 adds the
# driver). Track the generator, not its ~360 KB of derived output.
*.sav
