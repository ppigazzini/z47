{
  "extern_edges": 844,
  "note": "REPORT-28 s42: FROZEN non-regression guard, NOT a ratchet toward 0. core->shell extern edges are core files calling symbols upstream declares in its own src/c47 headers (465 of 469 = 99.1%); upstream is ONE library with no core/shell split, so this mirrors how upstream's own code is written and driving it to 0 would deviate from upstream in its hottest files. The gate asks only: did z47 ADD a coupling it did not have? The separate import-edge invariant (core @importing shell source) is genuine and stays at 0. 813->814: the 69c3ba4a2 resync added the upstream plotStatScale global (src/c47/plotstat.h); the core calc_state_backup owner persists it, mirroring the drawHistogram/plotStatMx plot-stat globals it already saves. 814->817: the de803c60b resync (1) adds the matrix-editor shadow cursor shadowI/shadowJ (matrixEditor.c globals) which calc_state_backup persists to backup.cfg beside the matrixIndex global it already saves (+2), and (2) routes keyboard_state_runtime's status-message lookup through the new errorMessageOf accessor (error.c packed errorMessages[][] into a pool behind it) (+1); all three are symbols upstream declares in its own headers and z47's core owners consume as upstream's single-library code does. 817->825: the b18a42df7 resync adds upstream-declared call topology: powReal/PowerComplex consult getInfiniteComplexAngle/setInfiniteComplexAngle (conversionAngles.h) for the new infinite-base sector results (+2); doLoad screens restored program memory via programMemoryHasOverlongLabelName (manage.h) and clears it with fnClPAll on refusal (+2); the fnLoadProgram first-pass screen consumes the shared step grammar paramTailBytes/literalTailBytes (nextStep.h) plus indexOfItems for the PTP classification (+3); calc_state_restore bounds file-supplied names through the new utf8ToStringWithLength (charString.h) (+1). All are symbols upstream declares in its own headers and calls on the same paths. 825->837: the 4697e526a resync adds upstream-declared call topology: differentiate.c's new deriv_pgm_variable walks a program's MVAR declarations via the shared step-walk primitives checkOpCodeOfStep/findNextStep/boundProgramNameLength (manage.h/nextStep.h, already core->shell edges elsewhere) and reads labelList (programming.h) directly (+5); differentiate.c's calcFuncValues abort-poison path polls exitKeyWaiting (addons.h), matching the same call already made by graph.c/solve.c (+1); integrate.c's and solve.c's engine-nesting guard clears thereIsSomethingToUndo (c47.h) on a refusal, the same global saveForUndo's own core->shell edge already sets (+2); the new boundShortIntegerWordSize/updateShortIntegerMasks (config.h) are consulted by every restore path (backup.cfg +2, the state file +1) that assigns shortIntegerWordSize directly, mirroring config.c's own fnSetWordSize call to updateShortIntegerMasks; stack.c's fnClearStack now calls calcModeNormal (flags.h), the same accessor keyboard_state_shared's core->shell edge already uses (+1); registerValueConversions.c's new saveRegisterSnapshot reads a short integer via getRegisterAsRawShortInt (registerValueConversions.h), the same accessor rotate_bits.zig's core->shell edge already uses (+1). All are symbols upstream declares in its own headers and calls on the same paths. 837->844: the 6559a9c59 resync adds upstream-declared call topology: fnSolveVar (solve.c) and fnIntVar (integrate.c) take the new currentDynamicSoftmenu (softmenus.h) so neither reads a dynamic menu's content while a static softmenu is on top (+2); doSave (saveRestoreCalcState.c) and the keyboard's user-key lookup read labels through the new getUserKeyLabelString (assign.h), which tolerates a refused userKeyLabel allocation, replacing the raw getNthString(userKeyLabel, ...) both already made (+2); restoreOneSection reports a refused pool allocation with displayCalcErrorMessage (error.h), the accessor every other restore refusal in the same file already uses (+1); derivativeEquation (differentiate.c) auto-assigns the formula's sole variable, which needs getNthString (softmenus.h) to walk the MVAR list and moreInfoOnError (error.h) to report the refusal (+2). All are symbols upstream declares in its own headers and calls on the same paths."
}
