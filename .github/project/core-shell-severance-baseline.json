{
  "extern_edges": 895,
  "note": "FROZEN non-regression guard, NOT a ratchet toward 0. core->shell extern edges are core files calling symbols upstream declares in its own src/c47 headers (465 of 469 = 99.1%); upstream is ONE library with no core/shell split, so this mirrors how upstream's own code is written and driving it to 0 would deviate from upstream in its hottest files. The gate asks only: did z47 ADD a coupling it did not have? The separate import-edge invariant (core @importing shell source) is genuine and stays at 0. 813->814: the 69c3ba4a2 resync added the upstream plotStatScale global (src/c47/plotstat.h); the core calc_state_backup owner persists it, mirroring the drawHistogram/plotStatMx plot-stat globals it already saves. 814->817: the de803c60b resync (1) adds the matrix-editor shadow cursor shadowI/shadowJ (matrixEditor.c globals) which calc_state_backup persists to backup.cfg beside the matrixIndex global it already saves (+2), and (2) routes keyboard_state_runtime's status-message lookup through the new errorMessageOf accessor (error.c packed errorMessages[][] into a pool behind it) (+1); all three are symbols upstream declares in its own headers and z47's core owners consume as upstream's single-library code does. 817->825: the b18a42df7 resync adds upstream-declared call topology: powReal/PowerComplex consult getInfiniteComplexAngle/setInfiniteComplexAngle (conversionAngles.h) for the new infinite-base sector results (+2); doLoad screens restored program memory via programMemoryHasOverlongLabelName (manage.h) and clears it with fnClPAll on refusal (+2); the fnLoadProgram first-pass screen consumes the shared step grammar paramTailBytes/literalTailBytes (nextStep.h) plus indexOfItems for the PTP classification (+3); calc_state_restore bounds file-supplied names through the new utf8ToStringWithLength (charString.h) (+1). All are symbols upstream declares in its own headers and calls on the same paths. 825->837: the 4697e526a resync adds upstream-declared call topology: differentiate.c's new deriv_pgm_variable walks a program's MVAR declarations via the shared step-walk primitives checkOpCodeOfStep/findNextStep/boundProgramNameLength (manage.h/nextStep.h, already core->shell edges elsewhere) and reads labelList (programming.h) directly (+5); differentiate.c's calcFuncValues abort-poison path polls exitKeyWaiting (addons.h), matching the same call already made by graph.c/solve.c (+1); integrate.c's and solve.c's engine-nesting guard clears thereIsSomethingToUndo (c47.h) on a refusal, the same global saveForUndo's own core->shell edge already sets (+2); the new boundShortIntegerWordSize/updateShortIntegerMasks (config.h) are consulted by every restore path (backup.cfg +2, the state file +1) that assigns shortIntegerWordSize directly, mirroring config.c's own fnSetWordSize call to updateShortIntegerMasks; stack.c's fnClearStack now calls calcModeNormal (flags.h), the same accessor keyboard_state_shared's core->shell edge already uses (+1); registerValueConversions.c's new saveRegisterSnapshot reads a short integer via getRegisterAsRawShortInt (registerValueConversions.h), the same accessor rotate_bits.zig's core->shell edge already uses (+1). All are symbols upstream declares in its own headers and calls on the same paths. 837->844: the 6559a9c59 resync adds upstream-declared call topology: fnSolveVar (solve.c) and fnIntVar (integrate.c) take the new currentDynamicSoftmenu (softmenus.h) so neither reads a dynamic menu's content while a static softmenu is on top (+2); doSave (saveRestoreCalcState.c) and the keyboard's user-key lookup read labels through the new getUserKeyLabelString (assign.h), which tolerates a refused userKeyLabel allocation, replacing the raw getNthString(userKeyLabel, ...) both already made (+2); restoreOneSection reports a refused pool allocation with displayCalcErrorMessage (error.h), the accessor every other restore refusal in the same file already uses (+1); derivativeEquation (differentiate.c) auto-assigns the formula's sole variable, which needs getNthString (softmenus.h) to walk the MVAR list and moreInfoOnError (error.h) to report the refusal (+2). All are symbols upstream declares in its own headers and calls on the same paths. 842->868: this iteration's gap-sweep repairs add upstream-declared call topology. EXPORTP was a shim that re-ran the save path; porting fnPExport/_fnExportProgram/_exportProgram gives the program owner the listing walk upstream writes there -- the step decoder and RTF converter decodeOneStep_XPORT/stringToRTF/stringToASCII (decode.h/charString.h), the step walk findNextStep/defineFirstDisplayedStep/getNumberOfSteps/_getProgramSize (nextStep.h/manage.h), the IR-printer arm printProgram plus lastFunc (print.h/c47.h), and the three listing-position globals firstDisplayedLocalStepNumber/programListEnd/lastProgramListEnd (c47.h) (+12). X.FN's FMA output moves off the realToSci upstream deleted onto realSCIToDisplayString (display.h) and narrows its separators through gapItemRight/indexOfItems (defines.h/items.h), which is the half of 6f4212ec8 the port had missed (+3). The 75-digit Taylor loop regains the interrupt and progress line its 1071-digit twin has, which reads explicitTaylorIterVisibilitySelection (wp34s.h) (+1); the integer-check commands push the LCD out through refreshLcd (screen.h) where the port had called the state refresh (+1). The remaining five are pass-1 repairs on the same footing: the plot overlay's graphAccActive and graphToRemainOnScreen (graphs.h/c47.h), and the three backup fields numLinesNumericFontBold/numScreensNumericFontBold/programmableMenu (screen.h/softmenus.h) the restore had been reading without saving. All are symbols upstream declares in its own headers and calls on the same paths. 868->878: the gap-sweep and progress-panel repairs since add upstream-declared call topology. The prime-factorisation command gains the listing upstream writes there -- it paints its progress over the stack with clearRegisterLine/showString (screen.h) and real34ToDisplayString (display.h), and its CSV export names the file through create_filename (c47Extensions/graphText.h) and filename_csv (c47.h) and reads the count back with fnStrtoReg (c47Extensions/addons.h) (+6). registers_compat_exports quotes a register in the six bug screens registers.c raises them from: getAngularModeName/getDataTypeName/getRegisterDataTypeName (debug.h), shortIntegerToDisplayString (display.h), and stringToASCII/stringToUtf8 (charString.h) (+6). shortint_runtime and register_metadata_runtime quote the same debug.h names in their own refusals, getRegisterDataTypeName and getDataTypeName (+2), and eigen reports its matrix through formatDoubleWidth (plotstat.h) (+1). calc_state_io moves off the errorMessages table onto the errorMessageOf accessor (c47.h) that replaced it (net 0). The one edge that is not an upstream-declared symbol is error_report -> printErrorTrace (+1): error.c's displayCalcErrorMessage ends in an OPTION_IR_PRINTING tail that touches only shell state, so the engine keeps the entry point and the shell keeps that tail behind this name -- z47's own split, not upstream's. Against those, six edges went away: stack_runtime_convert stopped reaching checkDateRange, checkTimeRange, convertDateRegisterToReal34Register, fnRefreshState and setLastintegerBasetoZero (-5), and register_metadata_tables stopped reaching compareString (-1). The progress panel's five display edges and circular_trig's explicitTaylorIterVisibilitySelection were not added, only moved: they now sit on the solve_runtime and command_wrappers/runtime seams the boundary guard approves, instead of on the owner files. 878->888: the 28fa9ad2e resync adds twelve upstream-declared edges and retires two. differentiate.c's derivative rework reaches four of them: the new findProgramLabel (manage.h) resolves PGMDRV's operand, deriv_open_mvar_menu puts the MVAR menu up with showSoftmenu (softmenus.h), deriv_tolerance_digits reads graphAccActive (graphs.h) so the ladder follows SDIGS while a graph is drawn, and deriv_formula_uses_delta/deriv_pgm_variable name the step variable with compareString (charString.h) (+4). solve.c's fnSolveVar compares the same name with compareString (+1), and the shared solve_runtime seam carries findProgramLabel for fnPgmSlv/fnPgmInt and the two sum/product argument checks (+1). saveRestoreCalcState.c's restore now recounts currentLocalStepNumber from the restored step POINTER, which needs the step walk findNextStep (nextStep.h), defineCurrentProgramFromCurrentStep (manage.h) to re-home a pointer outside the program, defineFirstDisplayedStep (nextStep.h) plus firstDisplayedLocalStepNumber (c47.h) to set the listing, and pemCursorIsZerothStep (c47.h), which the file does not carry (+5). saveRestorePrograms.c's new _restoreEditorPosition follows the step with defineCurrentProgramFromCurrentStep on the serialization seam (+1). All twelve are symbols upstream declares in its own headers and calls on the same paths. Against them, two went away: differentiate.zig stopped reaching findNamedLabel and letteredRegisterName when derivativeCommon was retired -- f' and f\" no longer resolve their own operand, findProgramLabel does it for them. 888->889: restoreCalc regains the `if(loadTestPrograms) return;` guard saveRestoreBackup.c has always had -- without it the backup.cfg read undoes the sample programs the reset just staged, which is what left the simulator's --writeexportall lane with nothing to export and both packaging jobs red. loadTestPrograms is c47.h's own global and upstream's copy of this function reads it exactly here. 889->891: the 7d3a60f27 resync adds two upstream-declared edges. Upstream's new set_def_Input_Default (c47Extensions/addons.h) clamps an Input_Default read from a file back to one of the four modes it recognises, and upstream calls it on all three restore paths; the two that live in core are calc_state_backup's backup.cfg read and calc_state_restore's state-file read (+2). The third call site, recall's config descriptor, is shell->shell and adds no edge. Both are a symbol upstream declares in its own header and calls on the same paths. 891->892: the 9af328a87 resync adds one upstream-declared edge. Upstream's new plotXYroute (c47Extensions/graphs.h) says a graph reached graph_plotmem through PLSTAT, which is the only route allowed to draw the 2LINE count-vs-value form, and upstream sets it in graph_stat -- solver/graph.c, a core owner here. It is a symbol upstream declares in its own header and sets on the same path. 892->894: the 2640d570a resync adds the two HEXGRP/BINGRP group widths (c47.h globals grpGroupingHex/grpGroupingBin) to all three persist owners and their default clamp grpGroupingHexBinDefault (config.h) to the two core restore paths (+8), while the two graph_dx/graph_dy globals upstream deleted from plotstat.h leave the same three owners (-6). All are symbols upstream declares in its own headers and saves, restores and clamps on the same paths. 894->897: the e9a15bd89 resync retires the GRAMOD reserved variable in favour of the plain byte global graMod (c47.h), which all three persist owners save, restore and clamp -- the same shape, and the same three owners, as the grpGroupingHex/grpGroupingBin pair the previous resync added (+3). It is a symbol upstream declares in its own header and persists on the same paths. 897->896: the c1a2ff7fd resync retires one edge and adds none. compare's owner stopped carrying a hand-copy of the SNAPVAL/RESTOREVAL macro pair and now calls saveRegisterSnapshot/restoreRegisterSnapshot the way the pinned compare.c does; that copy was register_compare.zig's only caller of getRegisterAsRawShortInt (registerValueConversions.h), and shortint_runtime.zig and register_conversions.zig still hold the same accessor on their own edges (-1). The two snapshot functions it reaches instead are exported by a core owner, so they are core->core and add nothing. 896->895: the c0e4bc05a resync retires one edge and adds none. calc_state_restore's named-variable read decodes the name with utf8ToStringWithLength bounded by MAX_LABEL_NAME_LENGTH+1, the way the pinned saveRestoreCalcState.c now does, so a long name cannot decode over the type string in aimBuffer; that owner already held utf8ToStringWithLength (charString.h) on four other calls, and this was its last caller of the unbounded utf8ToString (-1)."
}
