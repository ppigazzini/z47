{
 "absent": [],
 "drifted": {},
 "note": "Every menu_* table in softmenus.zig matches the pinned softmenus.c, and softmenu[] carries the same descriptors in the same ranks. Keep it that way: a resync that changes a menu must re-port the Zig table in the same commit, or this fails."
}
